[marianapparitions]
your-server.example.com ansible_user=ubuntu ansible_become=yes

# Alternative: Use IP address
# 192.168.1.100 ansible_user=ubuntu ansible_become=yes

[marianapparitions:vars]
ansible_python_interpreter=/usr/bin/python3

# If you need to specify SSH key:
# ansible_ssh_private_key_file=~/.ssh/your_key

# If you need to specify SSH port:
# ansible_port=22
